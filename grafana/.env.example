# QASL WEB3 LIVE SERVER — configuración del stack Grafana
# Copiá este archivo a .env si necesitás cambiar puertos o credenciales.

# Puerto de Grafana en el host
GRAFANA_PORT=48147

# Puerto de la API JSON en el host
API_PORT=48291

# Credenciales del admin de Grafana (cambiálas en entornos expuestos)
GRAFANA_USER=qasl
GRAFANA_PASSWORD=qaslweb32026
